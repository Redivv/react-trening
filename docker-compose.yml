version: '2'
services: 
  app:
    image: npm-react
    build: .
    ports: 
      - 3000:3000
    environment: 
      CHOKIDAR_USEPOLLING: 'true'
    volumes: 
      - ./:/home/node/app
      - /home/node/app/node_modules